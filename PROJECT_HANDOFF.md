# 鞋服 AI 合身与选购助手：POC / V1 项目交接说明

> 文档状态：POC 立项与消费者 V1 前置验证稿  
> 更新时间：2026-09-15  
> 面向对象：产品 Agent、前端 Agent、后端 Agent、计算机视觉 Agent、算法 Agent、评审 Agent  
> 当前阶段：移动端 H5 框架、尺码规则 POC、量体 Spike 和实时拍摄教练均已编码；下一步是真机校准拍摄阈值并输出身体比例区间

## 0. 当前版本记录（2026-09-15）

- 本地提交：`4a9e6dc`，加入浏览器端 MediaPipe Pose 实时拍摄教练。
- 已实现提示：手机竖屏/水平与俯仰、头脚完整入镜、人物大小、居中、正面站立、稳定状态。
- 降级路径：方向传感器不可用时继续用画面校正；模型下载失败时继续普通拍照；实验提示不会锁死拍摄按钮。
- 已验证：两个现有测试包共 14 项通过，JavaScript 语法检查和手机宽度布局通过；MediaPipe 运行库、WASM 与 Pose 模型地址均可访问。
- 尚未验证：真实 iPhone 相机、方向授权和不同人体/环境下的阈值准确性。
- 线上状态：GitHub Pages 仍为 `1a4d664`；本地提交推送时 GitHub 443 连接连续失败，恢复网络后执行 `git push origin main` 即可发布。
- 在线地址：<https://er18-w.github.io/ai-fit-lab-v1/>
- 回退点：`1a4d664`（加入实时拍摄教练之前）。

### 2026-09-15 第二次流程修正

- “我的”不再把资料表单和相机堆在同一页，改为档案总览、资料、拍摄说明、相机、比例结果五个步骤。
- 首页增加二维基础形象入口；拍摄原图负责比例分析，生成图只负责展示和换衣服。
- 进阶肩宽、胸围、腰围、臀围、臂长、腿内长、脚长和前掌宽全部改为选填；旧版假胸围/肩宽不会迁移到新档案。
- 人物居中阈值扩大，并加入连续六帧明显偏移才切换为不合格的缓冲逻辑。
- 拍摄后输出的是正面画面比例与置信度，不声称得到真实身体围度或 LiDAR 测距结果。
- 二维基础形象当前使用人工桥接：下载原图、交给 Codex 生成、再导回网页。自动生成服务尚未接入。

## 1. 项目目标

构建一个面向普通消费者的鞋服 AI 选购助手。用户建立身体档案后，可以上传衣服商品图、尺码表或商品截图，系统结合用户身体比例、商品尺寸和用户期望的穿着效果，输出：

- 推荐尺码；
- 肩线、胸围松量、衣长、袖长等版型判断；
- 适合或不适合的原因；
- 判断置信度及数据依据；
- 在用户照片上绘制的版型预测线；
- AI 生成的试穿视觉参考图。

长期愿景是发展为理解用户身体、审美、衣橱和目标的个人形象决策 Agent；V1 不实现完整长期愿景。

## 2. 核心产品原则

1. **宁少勿错**：缺少关键数据时降低置信度或拒绝给出具体结论，不编造尺寸。
2. **计算与生图分离**：规则引擎负责尺寸和版型判断，图像模型只负责视觉参考。
3. **结果必须可解释**：不能只给出“推荐 L 码”，必须说明肩宽差、胸围松量、衣长位置等依据。
4. **不承诺裁缝级量体**：普通摄像头输出尺寸区间和体态特征，不伪装成毫米级实测。
5. **先验证最难环节**：先验证摄像头量体和版型规则，再开发完整 UI、账号或商城能力。

## 3. 阶段范围

### 3.0 阶段定义

- **技术 POC**：只验证成人短袖 T 恤的量体、尺码表解析和版型解释，不等同于最终消费者产品。
- **消费者 V1**：仅在 POC 达标后，将能力包装为手机端完整流程，并加入可选视觉试穿与鞋履基础功能。

本文档中未特别说明时，开发任务优先指技术 POC。

### 3.1 第一验证品类

成人短袖 T 恤，包括正肩和落肩款。

暂不支持：

- 插肩袖和复杂连肩结构的精确肩线计算；
- 礼服、复杂裁剪、厚重羽绒服；
- 高弹紧身服装的精确压力与舒适度；
- 全品类服装统一推荐。

### 3.2 鞋履范围

鞋履不是第一条技术验证链路的重点。消费者 V1 或紧随其后的版本仅考虑：

- 左右脚长；
- 前掌宽；
- 商家尺码映射；
- 颜色搭配；
- 上脚视觉参考。

不判断鞋底软硬、足弓支撑、长期舒适度等主观体验。

### 3.3 产品形式

消费者 V1 暂定采用手机优先移动网页；是否增加 PWA 安装能力在 POC 后决定：

- 用户扫码或打开链接即可使用；
- 使用浏览器 `getUserMedia()` 在网页内请求后置摄像头；
- 使用 `<input capture="environment">` 作为兼容性降级方案；
- POC 和消费者 V1 不依赖 LiDAR、ARKit 或 ARCore Depth；
- 支持深度传感器的原生 App 作为后续增强路线。

选择移动网页的目的，是优先验证产品和算法闭环，而不是证明原生 AR 能力。PWA 不会额外获得 LiDAR 或更稳定的摄像头权限。

## 4. POC 与消费者流程

### 4.1 技术 POC 流程

```text
输入人工真值：身高、肩宽、胸围
  → 上传规范正面照片；侧面照片作为实验条件
  → 上传一张短袖 T 恤尺码表
  → 人工确认解析出的肩宽、胸围、衣长、袖长
  → 选择合体/微宽松/宽松
  → 输出各尺码对比、候选推荐、风险与依据
  → 输出人体关键点和尺寸关系示意带
  → 对比摄像头估算值与人工真值
```

POC 用于回答两个相互独立的问题：

1. 尺码表结构化与规则解释是否可用；
2. 摄像头估算是否足以减少用户手工输入。

摄像头量体失败不等于尺码推荐方案整体失败。

### 4.2 消费者 V1 目标流程

```text
进入手机网站
  → 创建身体档案
  → 输入常购男装/女装、身高、体重
  → 选择是否填写人工身体尺寸
  → 使用后置摄像头拍摄正面和侧面
  → 系统检查入镜、站姿、手机倾斜和清晰度
  → 生成身体比例与估算尺寸区间
  → 上传商品图与尺码表截图
  → OCR 和视觉模型解析商品数据
  → 用户确认识别结果
  → 选择期望效果：合体/微宽松/宽松
  → 规则引擎比较人体与商品数据
  → 输出推荐尺码、版型解释和置信度
  → 在真人照片上绘制尺寸关系示意带
  → 可选生成 AI 试穿参考图
  → 用户后续反馈实际穿着感受
```

## 5. 用户输入

### 5.1 必填

- 常购男装、女装或中性服装；
- 身高；
- 体重；
- 正面全身拍摄；
- 侧面全身拍摄；
- 当前商品希望达到的穿着效果。

“常购男装/女装”用于对应商品的号型和裁剪体系，不等同于审美风格。

### 5.2 选填

- 肩宽；
- 胸围；
- 腰围；
- 臀围；
- 臂长；
- 腿长。

产品提示：

> 系统可以通过摄像头估算身体比例和部分尺寸，但会存在一定误差。如果提供手工测量数据，尺码和版型判断会更准确。

### 5.3 输出硬门槛

- 有可靠人体胸围或商家人体推荐区间时，才可以输出具体尺码或明确候选顺序；
- 没有人工胸围、只有照片围度估算时，只输出候选尺码与风险，不承诺唯一正确尺码；
- 只有肩宽、身高和成衣尺寸时，可输出肩部与纵向效果，不输出完整“能否穿下”结论；
- 只有商家身高体重推荐时，以商家区间为主，系统只负责解释和有限修正；
- 将“可穿性判断”与“是否符合目标版型”作为两个独立结果。

技术 POC 中，人工肩宽和胸围作为评估真值必须采集；它们在未来消费者体验中仍可选填。

### 5.4 明确删除

V1 不要求用户录入“基准衣物”。

## 6. 摄像头量体目标

### 6.1 POC 需要输出

- 肩宽估算区间；
- 肩部倾斜程度；
- 上半身长度；
- 下半身长度；
- 臂长和腿长估算区间；
- 每项测量的置信度。

头身比、头肩比、肩臀比、腰线等审美分析字段保留在长期路线，不作为 POC 必出项。

### 6.2 预期技术路线

```text
后置摄像头视频/照片
  → 拍摄质量检测
  → 人体关键点检测
  → 人体分割轮廓
  → 头顶与脚底定位
  → 使用真实身高建立初始尺度
  → 按已冻结的相机内参、机位和站姿约束进行几何校正
  → 对同机位多帧做降噪；正侧面分别估算，不将其描述为完整三维重建
  → 输出身体比例、尺寸区间和误差范围
```

建议组件：

- MediaPipe Pose：人体关键点；
- MediaPipe 人体分割或其他轻量分割模型：身体轮廓；
- MediaPipe Face Mesh 或轮廓算法：面部和下颌定位；
- OpenCV：镜头校正、透视修正、像素尺度换算；
- Apple Vision/ARKit：未来原生 iOS 增强；
- ARCore Depth：未来原生 Android 增强。

### 6.3 精度边界

- 身高定标不自动消除透视误差；
- 姿态关键点不等于裁缝测量点；
- 宽松衣物会遮挡真实身体轮廓；
- 单张正面图不能可靠恢复胸腰臀围度；
- 输出应使用区间，例如 `44–46 cm`，而不是虚假精确值；
- 当误差过高时，提示用户填写人工数据。

### 6.4 测量定义

必须明确区分：

```text
body_biacromial_width_cm
  人体左右肩峰之间的直线宽度，人工真值口径

body_visual_shoulder_width_cm
  图像轮廓或视觉关键点估算出的肩部宽度

garment_seam_to_seam_shoulder_cm
  成衣平铺左右肩缝之间的距离
```

三者不能直接视为同一数值。只有经过真人数据验证的映射规则才能用于商品比较。

### 6.5 待冻结的拍摄协议

在实现量体前必须确定并版本化：

- 首轮支持 iOS Safari 与 Android Chrome；微信内置浏览器单独测试；
- 竖屏、后置 1× 主摄，禁止超广角；
- 手机高度、俯仰范围与用户距离范围；
- 全身占画面的目标比例；
- 正面和侧面站姿、手臂角度与贴身服装要求；
- 最低分辨率、光照与背景要求；
- 头顶、脚底、肩部遮挡时的自动拒绝条件；
- 同一用户重复拍摄次数和结果合并方式。

## 7. 商品输入与标准化

### 7.1 支持的商品输入

- 尺码表截图；
- 商品详情截图；
- 商品正面图；
- 商品背面图（可选）；
- 手工录入；
- 商品链接暂不作为 V1 必需能力，避免平台反爬、登录和页面变化问题。

### 7.2 商品数据类型

系统必须先识别数据属于哪一种：

1. 成衣实测尺寸；
2. 推荐人体尺寸范围；
3. 身高体重推荐；
4. 模特试穿信息；
5. 只有商品图片、没有尺寸。

不同数据类型对应不同能力和置信度。

### 7.3 上装标准字段

```json
{
  "measurement_type": "garment | body_range | recommendation",
  "unit": "cm | inch",
  "category": "t_shirt",
  "sleeve_type": "set_in | drop_shoulder | raglan | unknown",
  "fabric_stretch": "none | low | medium | high | unknown",
  "fabric_structure": "soft | medium | structured | unknown",
  "sizes": {
    "M": {
      "shoulder_cm": null,
      "chest_circumference_cm": null,
      "length_cm": null,
      "sleeve_length_cm": null,
      "cuff_circumference_cm": null
    }
  }
}
```

表级字段不足以保存所有歧义。每个原始测量字段还需记录：

```text
raw_label
raw_value
normalized_value
unit
basis: circumference | half_width | range | unknown
data_type: garment | body_range | recommendation | model_info
measurement_method
source_bbox
parser_confidence
user_confirmed
```

同一张尺码表可能同时包含成衣尺寸、建议身高体重和模特信息，不能只用一个表级 `measurement_type` 覆盖全部字段。

系统需要识别并统一以下差异：

- 胸围与平铺胸宽；
- 完整围度与半围；
- 英寸与厘米；
- 成衣尺寸与适合人体尺寸；
- 衣长、袖长的测量起点；
- 缺失字段；
- OCR 小数点和数字错误。

### 7.4 示例尺码表

当前样例为短袖 T 恤成衣尺寸：

| 尺码 | 肩宽 cm | 胸围 cm | 衣长 cm | 袖长 cm | 袖口 cm |
|---|---:|---:|---:|---:|---:|
| M | 55 | 107 | 68.5 | 19.5 | 33 |
| L | 56.5 | 110 | 70.5 | 20 | 34 |
| XL | 58 | 113 | 72.5 | 20.5 | 35 |
| 2XL | 59.5 | 117 | 74.5 | 21 | 36 |
| 3XL | 61 | 120 | 76.5 | 21.5 | 37 |

已观察到的尺码递增规律：

- 肩宽每码约增加 1.5 cm；
- 胸围每码约增加 3–4 cm；
- 衣长每码约增加 2 cm；
- 袖长每码约增加 0.5 cm；
- 袖口每码约增加 1 cm。

由于 M 码肩宽已经达到 55 cm，该商品很可能属于落肩或宽松设计。不得简单将“商品肩宽大于人体肩宽”判断为尺码过大。

### 7.5 尺码表解析流水线

```text
表格区域检测
  → 单元格 OCR 与行列坐标恢复
  → 字段语义和数据类型分类
  → 单位、半围/全围的确定性转换
  → 跨尺码增量和合理性校验
  → 歧义字段要求用户确认
  → 保存原始值、标准值、来源位置和确认记录
```

## 8. 合身计算逻辑

### 8.1 规则优先

V1 使用可解释规则引擎，不训练端到端尺码推荐模型，不让大语言模型直接执行数值计算。

### 8.2 正常装袖的尺寸关系近似

仅当肩缝明确且规则适用时：

```text
肩宽差 = 成衣肩宽 - 用户参考肩宽
单侧肩线偏移 ≈ 肩宽差 ÷ 2
```

该公式是水平尺寸关系近似，不代表肩缝沿人体曲面的真实位移。结果必须结合袖型解释：

- 正肩款：肩宽差用于判断向内、接近肩点或轻微外移；
- 落肩款：肩宽差用于描述落肩程度，不直接判定不合身；
- 插肩袖：不使用普通肩宽公式；
- 袖型未知：降低置信度。

### 8.3 胸腰臀松量

```text
胸围松量 = 成衣胸围 - 人体胸围
腰围松量 = 成衣腰围 - 人体腰围
臀围松量 = 成衣臀围 - 人体臀围
```

松量阈值必须按照品类、面料弹性、男装/女装和目标效果分别配置，不使用一套全品类常数。

### 8.4 衣长与袖长

将人体关键点映射为纵向区域：

- 腰线；
- 上髋；
- 髋部；
- 裆部；
- 肘部；
- 手腕。

根据成衣衣长和袖长预测下摆与袖口大致落点。测量起点不明确时降低置信度。

### 8.5 用户目标效果

用户对当前商品选择：

- 修身；
- 合体；
- 微宽松；
- 宽松；
- Oversize。

系统应在“能否穿下”和“是否符合目标效果”之间做区分。例如某尺码可以穿下，但不符合用户想要的正肩利落效果。

## 9. 置信度与拒答体系

短板原则可以作为内部安全门槛：

```text
总体置信度 = min(
  人体数据质量,
  商品数据质量,
  测量口径明确程度,
  规则适用程度
)
```

但不要把所有结果压缩成一个看似精确的概率。肩部、胸围、衣长、尺码推荐必须分别输出：

```text
结论
输入区间
数据质量
模型质量
规则适用性
用户可见置信等级
降级或拒答原因
```

必须先定义硬性拒答规则，再通过真人验证将内部质量因素映射为用户可见的高/中/低等级。

示例：

- 人工身体数据 + 完整成衣尺寸：高；
- 摄像头估算 + 完整成衣尺寸：中高；
- 人工身体数据 + 只有身高体重推荐：中低；
- 摄像头估算 + 只有商品照片：只做风格分析，不给精确肩线。

## 10. 视觉呈现

### 10.1 尺寸关系示意

在用户原始照片上绘制半透明位置范围，而不是看似精确的单像素缝线：

- 人体肩点；
- 预计商品肩缝；
- 袖口位置；
- 衣摆位置；
- 腰线和髋线。

该图承担“解释尺寸关系”的职责，不代表布料物理仿真。高置信度时显示预计落点范围带；中低置信度时只显示“正肩、轻落肩、明显落肩”等语义标签。

### 10.2 AI 试穿图

输入：

- 用户照片；
- 商品正面/背面图；
- 商品结构字段；
- 计算出的版型标签；
- 人物身份和身体比例保持要求。

建议首先测试 GPT Image 2.5 Sunburst；如果商品细节保持不足，再与专用 VTON 对照。

计算结果需转换成语义标签，例如：

```text
单侧肩线外移约 1–2 cm → 轻微落肩
胸围松量处于常规区间 → 常规宽松
衣摆接近髋点 → 常规衣长
```

不要期待图像模型稳定执行厘米级几何约束。生成图必须标明“视觉参考”，不得作为尺码计算依据。

## 11. 模型与组件建议

| 功能 | V1 建议 |
|---|---|
| 人体关键点 | MediaPipe Pose |
| 人体轮廓 | MediaPipe 分割或轻量人体分割模型 |
| 面部/头部比例 | Face Mesh + 人体轮廓 |
| 相机与几何校正 | OpenCV |
| 尺码表 OCR | PaddleOCR |
| 尺码表结构化 | 多模态语言模型 + 规则校验 |
| 袖型和商品视觉分类 | 视觉语言模型，低置信度时用户确认 |
| 合身推荐 | 确定性规则引擎 |
| 尺寸关系示意带 | Canvas/SVG 图层 |
| AI 试穿 | GPT Image 2.5，必要时比较专用 VTON |

## 12. 数据库建议

技术 POC 优先使用 JSON fixture 或 SQLite，避免数据库建设掩盖核心风险。消费者 V1 可使用 PostgreSQL，并可由 Supabase 托管；图片放对象存储。不需要独立向量数据库。

建议实体：

```text
users
body_profiles
body_measurements
fit_preferences
products
product_variants
garment_measurements
product_images
recommendations
tryon_jobs
fit_feedback
```

进入消费者 V1 后补充：

```text
measurement_sessions
measurement_definitions
measurement_observations
size_charts
size_chart_extractions
rule_versions
model_runs
consent_records
deletion_requests
```

推荐记录必须保存输入快照、规则版本、模型版本和证据字段，确保规则升级后仍能复现旧结果。

每个测量值必须记录：

```text
value
unit
source
measurement_method
confidence
min_value
max_value
created_at
```

身体照片属于敏感信息。需要明确授权、用途说明、删除机制、访问控制和尽量少保存原则。

## 13. 数据策略

### 13.1 不需要在 V1 前训练专用大模型

尺码表样本主要用于：

- 测试 OCR；
- 建立字段别名；
- 发现测量口径；
- 建立解析回归测试；
- 完善异常检测。

模型不会因为在聊天中不断上传图片就自动形成稳定产品能力。稳定性来自标准结构、规则、测试集和数据库。

### 13.2 初始验证数据集

建议目标，不是已完成结果：

1. 50–100 张不同格式的上装尺码表；
2. 20–30 位不同体型测试者的摄像头测量与软尺真值；
3. 若干件已知成衣尺寸 T 恤的真人试穿记录；
4. 统一记录肩缝位置、胸部松量、衣摆位置和主观版型评价。

### 13.3 后续核心数据

```text
人体档案
+ 商品成衣尺寸
+ 用户选择尺码
+ 实际穿着反馈
= 可持续改进的合身数据
```

## 14. V1 技术架构

```text
手机 H5/PWA
  ├─ 摄像头权限与拍摄引导
  ├─ 人体关键点质量检查
  ├─ 商品图/尺码表上传
  └─ 推荐与可视化结果

后端 API
  ├─ 用户与身体档案
  ├─ 图片上传与隐私控制
  ├─ OCR/视觉模型编排
  ├─ 商品数据标准化
  ├─ 合身规则引擎
  ├─ 置信度计算
  └─ AI 试穿任务

数据层
  ├─ PostgreSQL/Supabase
  └─ 对象存储
```

第一版可选择在浏览器端执行 MediaPipe，仅上传关键帧或计算结果；如服务端算法开发更快，也可以先上传经用户授权的照片处理，再逐步迁移到端侧。

## 15. 立项前自检结论

### 15.1 已确认可行

- 手机网页可以请求后置摄像头；
- 现有模型可以检测人体关键点和轮廓；
- OCR 和多模态模型可以解析大部分规范尺码表；
- 完整成衣尺寸可以支持可解释的版型比较；
- 规则结果可以绘制为肩线、袖口和衣摆示意；
- 图像模型可以生成风格和宽松等级参考图。

### 15.2 尚未验证

- 普通手机摄像头的肩宽和纵向尺寸误差；
- 不同设备、镜头和拍摄距离的稳定性；
- 二维体态特征与真人版型评价的一致性；
- 尺码表测量口径自动识别率；
- GPT Image 2.5 对人物体型和商品细节的保持率；
- 初始规则阈值能否跨不同 T 恤款式稳定工作。

## 16. 暂定验收目标

以下均为需要实验验证的目标，不是已有准确率：

- 尺码表分别评估字符准确率、行列关联、字段语义、单位/半围归一化和最终关键字段正确率；
- 摄像头肩宽估算中位绝对误差探索目标：≤2 cm；
- 肩宽 P90 绝对误差探索目标：≤3 cm；
- 同一用户重复测量误差探索目标：≤1.5 cm；
- 按设备、距离、机位和服装分别报告误差；
- 同时报告自动拒绝率，不允许只靠大量拒答美化准确率；
- 肩宽误差预计超过 3–4 cm 时，系统应要求人工补充；
- 正肩、轻微落肩、明显落肩三档与人工观察具有可用一致性；
- 数据不足时系统能够拒绝输出具体尺寸结论；
- 图像生成失败不影响计算结果；
- 每个推荐结论都能追溯到输入字段和规则。

## 17. 下一步执行顺序

不要先开发完整应用。先冻结字段定义、拍摄协议、输出合同、真值口径和隐私策略，然后并行执行三个风险 Spike：

1. **Spike A：人体量体**——固定拍摄条件下评估肩宽与纵向比例误差；
2. **Spike B：尺码表解析**——使用 10–20 张异构表格跑通端到端解析和用户确认；
3. **Spike C：生图保真**——用少量人物/商品对评估人物保持、商品保持、成本和耗时。

规则引擎可以并行使用人工人体真值和手工录入商品数据开发，不等待量体和 OCR 完成。

三个 Spike 形成结论后，再实现一个纵向技术原型：

```text
人物正侧面照片
  → 人体关键点与身体比例
  → 肩宽估算区间
  → 识别示例 T 恤尺码表
  → 对比 M/L/XL
  → 输出推荐尺码和版型解释
  → 绘制预测肩线
  → 可选生成试穿参考图
```

建议集成顺序：

1. 冻结字段和接口；
2. 开发离线人体测量实验；
3. 开发尺码表解析器；
4. 开发 T 恤合身规则引擎；
5. 开发照片版型线叠加；
6. 接入图像生成；
7. 将验证链包装成手机 H5/PWA；
8. 建立真人测试与回归测试。

“一个真人 + 一张尺码表”只能验证工程链路，不能验证推荐假设。最快的核心验证实验应使用一种短袖、3–5 个尺码、至少 10 位不同体型测试者，记录人工肩宽/胸围、规范照片、实际肩缝落点和主观版型评价。

## 18. 可并行子任务

### Track A：人体视觉测量

- 定义拍摄协议；
- 实现关键点与分割；
- 实现身高定标、镜头校正和多帧融合；
- 输出身体比例与误差区间；
- 建立软尺真值评估。

### Track B：商品数据解析

- 建立尺码表字段词典；
- OCR 图片；
- 判断成衣尺寸/人体范围/身高体重推荐；
- 处理半胸围、完整围度和单位转换；
- 建立 50–100 张表格回归集。

### Track C：合身规则与置信度

- 定义 T 恤袖型；
- 定义肩宽差、胸围松量、衣长和袖长规则；
- 设计修身到 Oversize 的目标映射；
- 建立数据不足时的降级策略；
- 输出结构化推荐理由；
- 定义输入/输出 JSON Schema、必填矩阵、缺失降级表、规则版本、`rule_id` 和解释模板。

### Track D：移动端产品原型

- 身体档案流程；
- 摄像头权限与拍摄引导；
- 商品上传与识别确认；
- 推荐结果页；
- 版型线叠加；
- 移动端兼容；PWA 安装能力推迟决定。

### Track E：视觉试穿评估

- 建立人物/商品测试对；
- 设计稳定提示词模板；
- 测试 GPT Image 2.5；
- 比较专用 VTON；
- 记录人物保持、商品保持和版型表达。

## 19. 评审 Agent 检查清单

评审时请明确区分：事实、假设、目标值和待验证事项。

重点检查：

1. 普通 RGB 摄像头量体是否被高估；
2. 身高定标与透视校正是否完整；
3. 尺码表字段和测量口径是否足够；
4. 肩宽差规则是否错误地处理落肩款；
5. 用户期望效果是否进入推荐逻辑；
6. 置信度是否能真实反映最弱数据源；
7. H5/PWA 是否足以完成 V1；
8. 是否把生成图误当成物理仿真；
9. 是否存在身体照片隐私与数据删除风险；
10. 哪个最小实验可以最快证伪项目核心假设。

## 20. 外部技术参考

- Apple Vision 3D 人体姿态：<https://developer.apple.com/documentation/vision/identifying-3d-human-body-poses-in-images>
- Apple ARKit Scene Depth：<https://developer.apple.com/documentation/arkit/arframe/scenedepth>
- Google ARCore Depth：<https://developers.google.com/ar/develop/depth>
- Google ARCore SDK：<https://github.com/google-ar/arcore-android-sdk>
- MediaPipe Pose Landmarks：<https://ai.google.dev/edge/api/mediapipe/python/mp/tasks/vision/PoseLandmark>
- OpenAI GPT Image 2.5：<https://developers.openai.com/api/docs/models/gpt-image-2.5-sunburst>
- OpenAI 图像生成指南：<https://developers.openai.com/api/docs/guides/image-generation>
- IDM-VTON：<https://arxiv.org/abs/2403.05139>
- CatVTON：<https://arxiv.org/abs/2407.15886>
- W3C Media Capture：<https://www.w3.org/TR/mediacapture-streams/>

## 21. 当前状态摘要

```text
产品方向：已收敛
V1 品类：已收敛为短袖 T 恤验证
产品形式：消费者 V1 暂定移动网页，PWA 待定
用户输入：已定义
商品输入：已定义
数据结构：初稿完成
合身规则：概念级，待实现与校准
人体量体：技术路线已选，精度待验证
视觉试穿：模型候选已选，效果待验证
数据库：消费者 V1 概念设计完成；POC 使用 JSON/SQLite
代码：尚未开始
测试数据：尚未建立
下一里程碑：冻结测量与接口合同，完成三项风险 Spike
```
