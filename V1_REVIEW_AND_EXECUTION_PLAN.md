# 鞋服 AI V1：评审结论与首轮执行计划

更新时间：2026-09-14

## 1. 评审结论

《鞋服_V1开发版.md》可以作为产品流程草案，但不能原样作为“所有参数已拍板”的开发规范。

总体路线可执行：移动端 H5 调用摄像头、浏览器端姿态识别、尺码表结构化、确定性规则计算、Canvas/SVG 示意以及云端虚拟试穿都可实现。当前最大风险仍是“普通 RGB 手机照片能否稳定得到足以支持尺码判断的身体测量值”，而不是页面或模型接口能否接通。

因此首轮不直接堆完整应用，先同时建设三块可独立验收的基础能力；每个未经真人或样本验证的常数都必须是可配置参数，不能伪装成事实。

## 2. 必须修正的事项

### P0：会改变结论可信度

1. MediaPipe 的左右肩点是姿态模型语义关键点，不是裁缝量体的肩峰点或成衣肩缝端点。字段必须分别命名，禁止统称“肩宽”。
2. MediaPipe 的 world landmarks 是模型估计坐标，不应直接当成手机测距仪给出的真实厘米。真实身高标定仍会受透视、站姿、头顶估计和脚点估计影响。
3. `1.12` 轮廓膨胀因子、`±8°` 机位阈值、`0.5` 腰线比例和四档松量区间都改为“待校准的配置默认值”，并记录版本与来源。
4. 量体测试必须同时报告中位误差、P90 误差、重复测量误差和自动拒绝率。5～10 人只能用于预实验，不能证明跨人群准确。
5. 没有用户胸围或可靠胸围数据时，只能输出候选尺码与肩部/衣长风险，不能给出唯一“准确推荐尺码”。
6. 尺码表不能只依赖一次 Vision LLM 输出。必须有 Schema 校验、数值合理性检查、半胸围/整圈口径检查和用户确认步骤。

### P1：工程与产品边界

1. 第一品类只支持成人短袖 T 恤；鞋履与其他上装暂不进入首轮开发。
2. “版型线”统一称为“尺寸关系示意带”，避免让用户误以为是布料物理仿真。
3. 摄像头和方向传感器都需要 HTTPS 与用户授权；方向权限存在浏览器差异，必须允许无传感器时继续，并改用拍摄引导和人工确认。
4. 云端试衣采用后端适配器和异步任务状态机，密钥不得进入前端。人物图与服装图应使用短期签名 URL，不建议永久公开。
5. 当前阿里云官方资料将 AI 试衣系列文档限定在华北 2（北京）；开发版中“基础版没有地域限制”的说法删除，正式接入时以账号控制台与最新官方文档实测为准。

## 3. 已冻结的首轮范围

### 输入

- 用户：身高、体重、正面全身拍摄；胸围为推荐具体尺码时的条件输入，腰围选填。
- 商品：成人短袖 T 恤尺码表截图；首轮允许用户确认识别结果。
- 偏好：修身、合体、微宽松、宽松。

### 输出

- 标准化尺码表及字段置信度。
- 每个尺码的肩部、胸围松量和衣长关系说明。
- 数据充分时给推荐尺码；数据不足时给候选尺码和补充信息提示。
- 人像上的尺寸关系示意带。
- AI 试穿只作为可选风格参考，不参与合身计算。

### 暂不实现

- 鞋履推荐、裤装、外套、女性复杂版型和儿童尺码。
- 从单张正面照片自动推算胸围/腰围。
- 布料垂坠、弹性、缩率的物理仿真。
- 用户账号、长期身体档案和跨设备同步。
- 生产环境计费、正式发布和商业化合规闭环。

## 4. 首轮并行任务

### Track A：量体风险 Spike

交付位置：`spikes/measurement/` 与 `docs/MEASUREMENT_CONTRACT.md`

交付物：

- 浏览器相机/上传照片两种输入的最小页面。
- 姿态关键点叠加、拍摄质量检查和可拒绝状态。
- 身高标定、肩部关键点距离、多帧汇总的纯函数接口。
- 明确区分身体关键点宽度、视觉体表宽度和成衣肩宽。
- 真人预实验数据模板与验收指标；默认常数集中配置。

通过标准：能够在无云服务的情况下复现同一输入的测量中间量；不能把未经验证的估算显示为精确厘米。

### Track B：尺码表 Schema 与合身规则

交付位置：`packages/fit-engine/`

交付物：

- 标准化商品 Schema、人体输入 Schema、推荐输出 Schema。
- 使用现有 M～3XL 示例尺码表的固定测试数据。
- 半胸围/整圈、单位、缺失字段和异常值校验。
- T 恤肩部、胸围松量、衣长的可配置规则与降级逻辑。
- Node 内置测试，不依赖外部模型或付费 API。

通过标准：同一结构化输入始终得到同一结果；缺胸围时不输出虚假的唯一精准尺码；每条结论带 `rule_id` 和输入依据。

### Track C：移动端 H5 演示壳

交付位置：`apps/web/`

交付物：

- 四步手机流程壳：建档与拍摄、尺码表确认、合身结果、可选试穿。
- 摄像头权限说明、失败状态、跳过方向传感器的降级路径。
- 使用本地 mock 数据跑通，不依赖账号和云 API。
- AI 试穿后端适配器接口占位与异步状态展示，不写入密钥。

通过标准：手机尺寸页面可打开并走完整流程；摄像头不可用、识别失败和生成失败时都有明确退路。

## 5. 集成门槛

三条线完成后先做一次集成评审，再决定是否接入真实 Vision LLM、对象存储与阿里云试衣。进入真实云服务前必须确认：

- 量体 Spike 至少完成小规模重复测量；
- Schema 与规则测试通过；
- H5 权限与错误流程在真实 iPhone/Android 上走通；
- 用户确认可接受阿里云账号、可能产生的费用以及人物照片上传第三方服务。

## 6. 第一阶段用户验收清单

- 手机浏览器能够打开页面并请求后置摄像头。
- 不授权摄像头时页面不会卡死，并能说明下一步。
- 示例尺码表能被显示为 M～3XL 的可编辑结构化数据。
- 输入身高、肩部测量值和胸围后，结果页能解释为什么推荐某一尺码。
- 不输入胸围时，系统诚实降级，不装作能精准推荐。
- 试穿失败不影响尺码和版型结果。

